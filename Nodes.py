class Nodes:
  def __init__(self, name, bound, citiesVisited, nodeMatrix, haveBeenToCity):
    self.name = name
    self.bound = bound
    self.citiesVisited = citiesVisited
    self.nodeMatrix = nodeMatrix
    self.haveBeenToCity = haveBeenToCity

